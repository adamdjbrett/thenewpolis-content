---
pagination:
  data: collections.authorPages
  size: 1
  alias: authorPage
permalink: /author/{{ authorPage.key }}/
---

<h1>{{ authorPage.name }}</h1>
<p><em>{{ authorPage.bio }}</em></p>

<h2>Posts by {{ authorPage.name }}</h2>

{% if authorPage.posts.length > 0 %}
  <ul>
  {%- for post in authorPage.posts -%}
    <li>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
      <br><small>{{ post.date | readableDate }}</small>
    </li>
  {%- endfor -%}
  </ul>
{% else %}
  <p>No posts found for this author.</p>
{% endif %}
